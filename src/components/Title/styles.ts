import styled from 'styled-components';

export const Container = styled.h1`
  display: flex;
  align-items: center;

  font-size: 3rem;
  font-family: 'Bitter', serif;

  img {
    margin-right: 1rem;
  }
`;
